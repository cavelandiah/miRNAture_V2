#!/bin/sh
log="${MIRNATURE_FAKE_LOG:-./nhmmer.args}"
printf '%s\n' "$0 $*" >> "$log"
out=""
while [ "$#" -gt 0 ]; do
  case "$1" in
    -o|--tblout) shift; out="$1" ;;
  esac
  shift
done
[ -n "$out" ] && { mkdir -p "$(dirname "$out")"; printf '# fake nhmmer output\n' > "$out"; }
exit 0
